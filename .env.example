# Read-only analytical inputs. Defaults to ../../location_selection/data/processed.
RLE_ANALYSIS_DATA_DIR=
# Optional read-only raw source directory for enumeration validation workflows.
RLE_RAW_DATA_DIR=
# Local-only server defaults.
RLE_HOST=127.0.0.1
RLE_PORT=8787

