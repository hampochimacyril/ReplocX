.DS_Store
__pycache__/
*.py[cod]
.env
.env.*
.venv/
.tools/
node_modules/
dist/
coverage/
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/
blob-report/
playwright-report/
test-results/
data/analysis_manifest.json
data/private/
data/raw/

# Credential-bearing documents kept locally; never commit.
*.docx
~$*.docx

# Pipeline outputs/intermediates are regenerated locally. Real analytical
# outputs stay outside Git and are mounted read-only on approved private hosts.
pipeline/out/
pipeline/work/
pipeline/cache/
# Tract-level pilot outputs are partial and regenerated by
# `python3 -m pipeline.pilot`; they are not a committed dataset.
pipeline/pilot_out/
*.zip
*.xlsx
*.parquet

# Transient marketing build artifacts (regenerated by generate_promo_video.py)
marketing/_frames/
marketing/_bed.wav
marketing/*.wav
marketing/*.mp3
marketing/ReplocX_demo_*_narrated.mp4
marketing/captions.srt
